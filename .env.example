# Database Connection Configuration
# (Can be set in Render / Vercel Environment Variables dashboard)

# Option 1: Individual Variables
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=chr_db
DB_PORT=3306
DB_SSL=false

# Option 2: Full Connection String (if provided by your database provider)
# DATABASE_URL=mysql://username:password@hostname:3306/chr_db
